{
  "name": "competitor-analyzer",
  "private": true,
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,magenta \"npm run dev --prefix web\" \"npm run dev --prefix server\"",
    "build": "npm run build --prefix server && npm run build --prefix web",
    "install:all": "npm install --prefix server && npm install --prefix web"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
